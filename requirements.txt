gradio>=6.0
# PyTorch：CPU 版直接 pip install torch；有 NVIDIA GPU 建议装 CUDA 版以加速：
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
torch>=2.0
torchvision>=0.15
numpy>=1.24
matplotlib>=3.7
scikit-learn>=1.3
Pillow>=9.0
requests>=2.28
